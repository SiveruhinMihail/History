# History - Сообщество историков

> Полнофункциональная платформа для историков: посты, комментарии, лайки, сообщества, модерация, аналитика и многое другое.

## 📋 Содержание
- [О проекте](#-о-проекте)
- [Технологический стек](#-технологический-стек)
- [Основные возможности](#-основные-возможности)
- [Структура проекта](#-структура-проекта)
- [Установка и запуск](#-установка-и-запуск)
- [Настройка Supabase](#-настройка-supabase)
- [Переменные окружения](#-переменные-окружения)
- [Скрипты](#-скрипты)
- [API endpoints](#-api-endpoints)
- [Модерация и жалобы](#-модерация-и-жалобы)
- [Сообщества и рейтинг](#-сообщества-и-рейтинг)
- [Умная сортировка](#-умная-сортировка)
- [Лицензия](#-лицензия)

## 🧐 О проекте
History Forum — это современное веб-приложение, созданное для профессионального и любительского общения историков. Платформа позволяет публиковать научные и популярные статьи, обсуждать их в комментариях, объединяться в тематические сообщества, оценивать контент и модерировать его. Проект реализован на **Nuxt 4** с бэкендом **Supabase** и строгой типизацией TypeScript.

## 💻 Технологический стек

| Категория       | Технологии                                                                                   |
|-----------------|-----------------------------------------------------------------------------------------------|
| Frontend        | Nuxt 4 (Vue 3), TypeScript, Tailwind CSS, Heroicons, FormKit, Chart.js, markdown-it, Cropper.js |
| Backend         | Supabase (PostgreSQL, Storage, Realtime, Auth)                                               |
| Инструменты     | ESLint, Prettier, Supabase CLI, Git                                                          |

## ✨ Основные возможности

### Пользователи
- Регистрация, вход, выход, сброс пароля (Supabase Auth).
- Профили с аватаркой (загрузка и обрезка изображения), именем, описанием в Markdown.
- Уровень пользователя, рассчитываемый на основе рейтинга.
- Роли: обычный пользователь, модератор, администратор.

### Посты
- Создание постов с заголовком, описанием (Markdown) и несколькими изображениями.
- Лайки и добавление в избранное (закладки).
- Категоризация постов (можно выбирать несколько категорий).
- Модерация: новые посты получают статус `pending`, одобряются или отклоняются модераторами.

### Комментарии
- Вложенные комментарии (ответы) с возможностью прикреплять изображения.
- Лайки комментариев.
- Жалобы на комментарии, посты и пользователей.
- Real-time обновления при добавлении новых комментариев.

### Категории
- Просмотр всех категорий с количеством постов в каждой.
- Страница категории со списком постов (пагинация, сортировка).
- На главной отображаются только категории, в которых есть одобренные посты.

### Сообщества
- Создание сообществ с названием, описанием и аватаром.
- Участники делятся на администраторов, членов и ожидающих (pending).
- Заявки на вступление одобряются администраторами.
- Внутренний чат сообщества (только для участников).
- Рейтинг сообщества начисляется за лайки под комментариями его участников (связь через таблицу `comment_communities`, заполняемую триггером).

### Модерация и жалобы
- Отдельные страницы для модерации постов (`/moderate/posts`) и жалоб (`/moderate/reports`).
- Возможность удалить контент (пост, комментарий) или заблокировать пользователя (два вида бана: только блокировка аккаунта или блокировка с удалением всех постов и комментариев).
- Жалобы от пользователей блокируются после отправки (нельзя повторно пожаловаться на один объект).

### Активность и статистика
- В профиле отображается статистика: количество постов, комментариев, полученных и отданных лайков.
- График активности за последние 30 дней (лайки и комментарии по дням) с использованием Chart.js.

### Поиск
- Глобальный поиск по категориям и постам на главной странице.

### Умная сортировка
- Посты на главной и в категориях сортируются сначала по популярности (количество лайков), затем по дате (новые сверху). Переключатель сортировки отсутствует – алгоритм фиксирован.
- Комментарии сортируются по количеству лайков (поле `likes_count`), затем по дате (новые сверху). Поле обновляется триггерами.

## 📁 Структура проекта
```
history-forum/
├── app/                      # Nuxt-приложение
│   ├── components/           # Vue-компоненты
│   │   ├── AvatarUpload.vue
│   │   ├── CategoryRow.vue
│   │   ├── CommentForm.vue
│   │   ├── CommentItem.vue
│   │   ├── Dropdown.vue
│   │   ├── Header.vue
│   │   ├── ImageViewer.vue
│   │   ├── PostCard.vue
│   │   ├── ProfileTabs.vue
│   │   ├── ReportModal.vue
│   │   └── UserPosts.vue
│   ├── composables/          # Логика (composables)
│   │   ├── useAuth.ts
│   │   ├── useCommunity.ts
│   │   ├── useFavorites.ts
│   │   ├── usePosts.ts
│   │   ├── useStorage.ts
│   │   └── useUser.ts
│   ├── layouts/              # Шаблоны страниц
│   │   └── default.vue
│   ├── pages/                # Страницы
│   │   ├── auth/             # Аутентификация
│   │   ├── categories/       # Категории
│   │   ├── communities/      # Сообщества
│   │   ├── moderate/         # Модерация
│   │   ├── post/             # Пост
│   │   ├── profile/          # Профиль
│   │   ├── create.vue
│   │   └── index.vue
│   └── server/               # Серверные API (в app/server)
│       └── api/
│           └── moderation/
│               ├── approve-post.post.ts
│               ├── ban-user-only.post.ts
│               ├── ban-user-with-content.post.ts
│               ├── delete-comment.post.ts
│               ├── delete-post.post.ts
│               └── resolve-report.post.ts
├── types/                    # Сгенерированные типы Supabase
│   └── supabase.ts
├── .env.example
├── .eslint.config.mjs
├── nuxt.config.ts
├── package.json
└── README.md
```

## 🚀 Установка и запуск

### Требования
- Node.js 18+
- npm / yarn / pnpm
- Аккаунт Supabase (бесплатный)

### Клонирование репозитория
```bash
git clone https://github.com/yourusername/history-forum.git
cd history-forum
```

### Установка зависимостей
```bash
npm install
```

### Настройка Supabase
1. Создайте новый проект в [Supabase](https://supabase.com).
2. Выполните SQL-скрипты для создания таблиц и триггеров (предоставлены в документации проекта).
3. Включите аутентификацию по email/паролю.
4. Создайте бакеты `avatars` и `posts` в Storage (публичные).

### Переменные окружения
Создайте файл `.env` в корне проекта:

```env
NUXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NUXT_SERVICE_KEY=your-service-role-key
```

Получить ключи можно в разделе **Project Settings → API**.

### Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:3000`.

### Сборка для продакшена
```bash
npm run build
npm run preview
```

## 🔧 Переменные окружения

| Переменная | Описание |
|------------|----------|
| `NUXT_PUBLIC_SUPABASE_URL` | URL вашего Supabase проекта |
| `NUXT_PUBLIC_SUPABASE_ANON_KEY` | Публичный анонимный ключ |
| `NUXT_SERVICE_KEY` | Сервисный ключ (service_role) для серверных эндпоинтов (требуется для бана и удаления) |

## 📦 Скрипты
- `npm run dev` – запуск dev-сервера
- `npm run build` – сборка проекта
- `npm run preview` – предпросмотр продакшен-сборки
- `npm run lint` – проверка кода ESLint
- `npm run lint:fix` – автоматическое исправление ошибок ESLint
- `npm run format` – форматирование кода Prettier

## 🌐 API endpoints
Серверные эндпоинты (используются для модерации, бана и т.д.) расположены в `server/api/moderation/`. Все они принимают POST-запросы и требуют авторизации модератора/админа.

- `/api/moderation/approve-post` – одобрить пост (`{ postId }`)
- `/api/moderation/reject-post` – отклонить пост (`{ postId }`)
- `/api/moderation/delete-post` – удалить пост (`{ postId }`)
- `/api/moderation/delete-comment` – удалить комментарий (`{ commentId }`)
- `/api/moderation/ban-user-only` – заблокировать пользователя (`{ userId }`)
- `/api/moderation/ban-user-with-content` – заблокировать и удалить весь контент пользователя (`{ userId }`)
- `/api/moderation/resolve-report` – закрыть жалобу (`{ reportId, status }`)

## 🛡️ Модерация и жалобы
- Посты перед публикацией проходят проверку модератором (статус `pending`).
- Пользователи могут пожаловаться на любой контент (пост, комментарий, другого пользователя). После отправки жалобы кнопка блокируется.
- Жалобы отображаются на странице `/moderate/reports`. Модератор может удалить контент, заблокировать автора или отклонить жалобу.
- Бан пользователя осуществляется через серверные эндпоинты с использованием `service_role`, что обходит RLS. Доступны два типа бана: только блокировка аккаунта и блокировка с удалением всех постов и комментариев.

## 👥 Сообщества и рейтинг
- Создание сообществ доступно всем аутентифицированным пользователям.
- Вступление требует одобрения администратора (заявка с ролью `pending`).
- Администраторы могут одобрять или отклонять заявки.
- Чат сообщества доступен только участникам (реальное время через Supabase Realtime).
- Рейтинг сообщества увеличивается за каждый лайк, поставленный комментарию участника сообщества (на момент написания комментария). Связь комментариев с сообществами хранится в таблице `comment_communities`, заполняемой триггером.

## 🧠 Умная сортировка
- **Посты**: на главной и в категориях сортируются по полю `rating` (количество лайков) в убывающем порядке, затем по `created_at` (новые сверху).
- **Комментарии**: сортируются по полю `likes_count` (количество лайков) в убывающем порядке, затем по `created_at` (новые сверху). Поле `likes_count` обновляется триггерами при добавлении/удалении лайков.

## 📄 Лицензия
MIT © [Ваше имя]

---

**Автор:** [Lum1nous]  
**Дата:** 24.02.2026  
**Репозиторий:** [https://github.com/SiveruhinMihail/History](https://github.com/SiveruhinMihail/History)
```